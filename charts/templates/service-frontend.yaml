apiVersion: v1
kind: Service
metadata:
    name: frontend
    namespace: oootp
    labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/part-of: oootp
spec:
    selector:
        app.kubernetes.io/name: frontend
    ports:
        - name: http
          port: 8080
          targetPort: 8080
          protocol: TCP
