apiVersion: v1
kind: Service
metadata:
    name: backend
    namespace: oootp
    labels:
        app.kubernetes.io/name: backend
        app.kubernetes.io/part-of: oootp
spec:
    selector:
        app.kubernetes.io/name: backend
    ports:
        - name: http
          port: 3333
          targetPort: 3333
          protocol: TCP
